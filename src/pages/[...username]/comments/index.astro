---
import Layout from '@lay/Layout.astro';

//function capitalizeFirstLetter(str) {
  //if (!str) return str;

  //return str.charAt(0).toUpperCase() + str.slice(1);
//}

const name = 'Dex';

const username = 'dex';

//const theusername = capitalizeFirstLetter(username);

//let parts = name.split(' ')
//let firstName = parts.shift();
//let lastName = parts.join(' ');
---
<Layout title={`${name}'s Friend Comments`}>
    <main class="mx-[110px] mb-32">
        <h1 class="text-[25.6px] font-semibold mb-2 text-balance">{`${name}'s Friend Comments`}</h1>
        <a class="text-[#1D4ED8] hover:text-[#ED0707] hover:underline text-xs" href={`/${username}`}>« Back to {name}'s profile</a>
    </main>
</Layout>