---
import Layout from '@lay/Layout.astro';

import SmallLink from '@com/main/SmallLink.astro';

const username = 'astro';
const hidden = false;

//let parts = name.split(' ')
//let firstName = parts.shift();
//let lastName = parts.join(' ');
---
<Layout title="Your Blog">
	<main class="mx-[110px] mb-8 text-black">
		<h1 class="text-[19.2px] font-semibold mb-2">Your Blog</h1>
		{ hidden ? '' : <p class="text-xs mb-2"><SmallLink href={`/${username}/blog/edit`} link="edit blog" /></p> }
		<a class="text-blue-600 hover:text-red-600 hover:underline text-xs" href={`/${username}`}>« Back to your profile</a>
	</main>
</Layout>