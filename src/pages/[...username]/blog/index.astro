---
import OtherLayout from '@lay/OtherLayout.astro';

import SmallLink from '@com/main/SmallLink.astro';

const username = 'dex';

const name = 'Dex';

import loggedin from '../../api/loggedin.json';

const hidden = true;

//let parts = name.split(' ')
//let firstName = parts.shift();
//let lastName = parts.join(' ');
---
<OtherLayout title=`${name}'s Blog` pagetitle=`${name}'s Blog`>
	{ loggedin ? <p class="text-xs mb-2"><SmallLink href={`/${username}/blog/edit`} link="edit blog" /></p> : null }
	<a class="text-blue-600 hover:text-red-600 hover:underline text-xs" href={`/${username}`}>« Back to { hidden ? `Dex's` : 'your' } profile</a>
</OtherLayout>