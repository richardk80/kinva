---
import Layout from '@lay/Layout.astro';

const username = '';

//let parts = name.split(' ')
//let firstName = parts.shift();
//let lastName = parts.join(' ');
---
<Layout title="Delete Account">
	<main class="mx-[50px] mb-8">
		<h1 class="text-[25.6px] font-semibold mb-2 text-balance">Delete Account</h1>
		<a class="text-[#1D4ED8] hover:text-[#ED0707] hover:underline text-xs" href={`/${username}`}>« Back to your profile</a>
	</main>
</Layout>