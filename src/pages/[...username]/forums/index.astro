---
import OtherLayout from '../../@lay/OtherLayout.astro';

const name = 'Dex';

const username = 'dex';

//let parts = name.split(' ')
//let firstName = parts.shift();
//let lastName = parts.join(' ');
---
<OtherLayout title="Your Forums">
	<h1 class="text-[25.6px] font-semibold mb-2 text-balance">Your Forums</h1>
	<a class="text-blue-600 hover:text-red-600 hover:underline text-xs" href={`/${username}`}>« Back to {name}'s profile</a>
</OtherLayout>