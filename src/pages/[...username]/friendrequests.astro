---
import OtherLayout from '@lay/OtherLayout.astro';

const username = 'dex';

//let parts = name.split(' ')
//let firstName = parts.shusernamelet lastName = parts.join(' ');
---
<OtherLayout title="Friend Requests" pagetitle="Friend Requests">
	<a class="text-neutral-900 hover:text-red-600 hover:underline text-xs" href={`/${username}`}>« Back to your profile</a>
</OtherLayout>