---
const userCookie = Astro.cookies.get('user');
const users = userCookie ? JSON.parse(userCookie.value) : null;
if (!users) return Astro.redirect("/");

import OtherLayout from '@lay/OtherLayout.astro';

//function capitalizeFirstLetter(str) {
  //if (!str) return str;

  //return str.charAt(0).toUpperCase() + str.slice(1);
//}

const name = 'Dex';

const username = 'dex';

//const theusername = capitalizeFirstLetter(username);

//let parts = name.split(' ')
//let firstName = parts.shift();
//let lastName = parts.join(' ');
---
<OtherLayout title={`${name}'s Friend Feedback`} pagetitle={`${name}'s Friend Feedback`}>
  <a class="text-neutral-950 hover:text-red-600 hover:underline text-xs" href={`/${username}`}>« Back to {name}'s profile</a>
</OtherLayout>