---
import OtherLayout from '../@lay/OtherLayout.astro';

const name = 'Dex';

const username = 'dex';

//let parts = name.split(' ')
//let firstName = parts.shusernamelet lastName = parts.join(' ');
---
<OtherLayout title={`${name}'s Friends List`}>
	<h1 class="text-[25.6px] font-semibold mb-2 text-balance">{name}'s Friends List</h1>
	<a class="text-blue-600 hover:text-red-600 hover:underline text-xs" href={`/${username}`}>« Back to <span class="capitalize">{username}</span>'s profile</a>
</OtherLayout>