---
import Layout from '@lay/Layout.astro';

const name = 'Dex';

const username = 'dex';

//let parts = name.split(' ')
//let firstName = parts.shusernamelet lastName = parts.join(' ');
---
<Layout title={`${name}'s Friends List`}>
	<main class="mx-[50px] mb-8">
		<h1 class="text-[25.6px] font-semibold mb-2 text-balance">{name}'s Friends List</h1>
		<a class="text-[#1D4ED8] hover:text-[#ED0707] hover:underline text-xs" href={`/${username}`}>« Back to <span class="capitalize">{username}</span>'s profile</a>
	</main>
</Layout>