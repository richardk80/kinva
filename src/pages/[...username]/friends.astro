---
import OtherLayout from '@lay/OtherLayout.astro';

const name = 'Dex';

const username = 'dex';

//let parts = name.split(' ')
//let firstName = parts.shusernamelet lastName = parts.join(' ');
---
<OtherLayout title={`${name}'s Friends List`} pagetitle={`${name}'s Friends List`}>
	<a class="text-neutral-950 hover:text-red-600 hover:underline text-xs" href={`/${username}`}>« Back to <span class="capitalize">{username}</span>'s profile</a>
</OtherLayout>