---
const username = 'astro';
---
<p class="text-xs font-semibold">My URL:
    <a class="text-xs font-semibold text-blue-600 hover:text-red-600 hover:underline" href={`/${username}`}>/{username}</a>
</p>