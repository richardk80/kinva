---
const username = 'dex';
---
<p class="text-xs font-semibold">My URL:
    <a class="text-xs font-semibold text-neutral-900 hover:text-red-600 hover:underline" href={`/${username}`}>/{username}</a>
</p>