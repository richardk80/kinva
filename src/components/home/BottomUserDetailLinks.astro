---
const { username } = Astro.props;
---
<p class="text-xs font-semibold">View:
    <a class="text-xs font-semibold text-blue-600 hover:text-red-600 hover:underline" href={`/${username}/friends`}>Friends</a>
    <span class="text-xs font-medium">-</span>
    <a class="text-xs font-semibold text-blue-600 hover:text-red-600 hover:underline" href={`/${username}/blocked`}>Blocked</a>
</p>