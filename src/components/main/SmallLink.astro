---
export interface Props {
    href?: string;
    link?: string;
    classname?: string;
}

const { href, link, classname = "" } = Astro.props;
---
[<a class={`text-blue-600 hover:text-red-600 hover:underline ${classname}`} href={href}>{link}</a>]